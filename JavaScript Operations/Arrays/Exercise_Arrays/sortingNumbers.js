function solve(array){

    let result = [];

    


    // for(var i = 0; i < array.length; i++){

    //     let currentSmallest = Math.min(...array);
    //     let currentBiggest = Math.max(...array);

    //     let indexCurrentSmallest = array.indexOf(currentSmallest);
    //     let indexCurrentBiggest = array.indexOf(currentBiggest);

    //     result.push(currentSmallest);
    //     result.push(currentBiggest);

    //     arrays.splice(arrays.indexOf(currentSmallest), 1);
    //     arrays.splice(arrays.indexOf(currentBiggest), 1);

    // }

    // return result;
}
solve([1, 65, 3, 52, 48, 63, 31, -3, 18, 56]);